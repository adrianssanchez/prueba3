@model IEnumerable<AppGestionEMS.Models.Evaluaciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Curso.actual)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nota)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curso.actual)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nota)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { curso = item.CursoId, user = item.UserId }) |
            @Html.ActionLink("Details", "Details", new { curso = item.CursoId, user = item.UserId }) |
            @Html.ActionLink("Delete", "Delete", new { curso = item.CursoId, user = item.UserId })
        </td>
    </tr>
}

</table>
